name: Deploy

on:
  push:
    branches:
      - feature/deploy-dev # TODO change to main
  workflow_run:
    workflows: ["CI"]
    types: completed
    branches: feature/deploy-dev # TODO change to main

jobs:
  check:
    runs-on: ubuntu-latest
    if: ${{ github.event.workflow_run.conclusion == 'success' }}
    steps:
      - name: Deploy
        run: |
          echo "Deploying..."
