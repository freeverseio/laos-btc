apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: laos-btc-{{ .Values.bitcoind.network }}
  labels:
    app: laos-btc
spec:
  selector:
    matchLabels:
      app: laos-btc
  serviceName: "laos-btc-headless"
  replicas: 1
  template:
    metadata:
      labels:
        app: laos-btc
    spec:
      containers:
      - name: laos-btc
        image: freeverseio/laos-btc:main
        imagePullPolicy: Always # The main image tag is updated whenever a feature gets merged
        args:
          - --data-dir
          - /data
          - --bitcoin-rpc-url
          - http://bitcoind-{{ .Values.bitcoind.network }}:{{ .Values.bitcoind.rpcPort }}
          - --bitcoin-rpc-username
          - $(LAOS_BTC_RPC_USER)
          - --bitcoin-rpc-password
          - $(LAOS_BTC_RPC_PASS)
          {{- if eq .Values.bitcoind.network "testnet" }}
          - --testnet
          {{- end }}
          - server
          - --http-port
          - {{ .Values.laosbtc.httpPort | quote }}
        ports:
        - containerPort: {{ .Values.laosbtc.httpPort }}
          name: http
        volumeMounts:
        - name: data
          mountPath: /data
        env:
        - name: LAOS_BTC_RPC_USER
          valueFrom:
            secretKeyRef:
              name: bitcoin-rpc-auth
              key: rpcuser
        - name: LAOS_BTC_RPC_PASS
          valueFrom:
            secretKeyRef:
              name: bitcoin-rpc-auth
              key: rpcpassword
      tolerations:
        - key: "ord-only"
          operator: "Equal"
          value: "true"
          effect: "NoSchedule"
  volumeClaimTemplates:
  - metadata:
      name: data
    spec:
      storageClassName: do-block-storage
      accessModes: [ "ReadWriteOnce" ]
      resources:
        requests:
          storage: {{ .Values.laosbtc.storageSize }}
